%section.register.container
  = bootstrap_form_for(@user, control_col: "col-med-2") do |f| 
    = f.error_summary 
    = f.text_field :full_name
    = f.email_field :email
    = f.password_field :password
    = f.submit "Sign Up"

